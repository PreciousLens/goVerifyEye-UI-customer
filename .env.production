# Production builds call the API via same-origin `/api/...`.
# Vercel rewrites those requests to https://goverifeye-api.onrender.com (see vercel.json).
# This avoids browser CORS against Render.
VITE_API_BASE_URL=same-origin
